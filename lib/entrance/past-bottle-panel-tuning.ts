/**
 * 過去ボトル選択 — リスト UI の位置。
 * 調整はこのファイルだけ触ればよい。
 */

export const PAST_BOTTLE_PANEL_TUNING = {
  /** パネル配置領域 — 上端（%・過去ボトルリンク下を空ける） */
  topInsetPercent: 24,
  /** パネル配置領域 — 下端（%） */
  bottomInsetPercent: 8,
  /** 横 padding（px） */
  horizontalPaddingPx: 20,
} as const;
