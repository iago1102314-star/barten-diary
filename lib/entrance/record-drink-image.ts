/** 録音シーン用 — 現状すべてこの画像（typo ファイル名のまま） */
export const RECORD_DRINK_IMAGE_PATH = "/assets/drinks/old-fashoned.webp";

export function getRecordDrinkImagePath(): string {
  return RECORD_DRINK_IMAGE_PATH;
}
